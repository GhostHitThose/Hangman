package me.maxrenner.ux.buttons;

import java.awt.*;

public class PlayButton extends Button {
    public PlayButton(int x, int y, int width, int height, Color color, Color hoveredColor, boolean visible) {
        super(x, y, width, height, color, hoveredColor, visible);
    }

    @Override
    public void onClick() {

    }
}
